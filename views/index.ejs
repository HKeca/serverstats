<!DOCTYPE html>  
<html lang="en">  
    <head>
        <meta charset="utf-8">
        <title>McMMO Stats</title>
        <meta name="description" content="mcmmo stats for my server">
        <meta name="author" content="Hunter Keca">

        <!-- HTML5 shim, for IE6-8 support of HTML elements -->
        <!--[if lt IE 9]>
        <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
        <![endif]-->

        <!-- styles -->
        <link href="https://fonts.googleapis.com/css?family=Roboto+Mono" rel="stylesheet">
        <link rel="stylesheet" type="text/css" href="/assets/main.css">
    </head>
    <body>
        <h1 class="title">
            Player Stats
        </h1>

        <div class="container">
            <% for(let i = 0; i < players.length; i++) {%>
            <div class="playerStats">
                <p>
                    <strong><%= players[i].user %>:</strong> <%= powerLevels[i] %>
                </p>
                <div>
                    <a href="#" class="link">[+]</a>

                    <ul tabindex="0" class="details" onblur="closeEl(this);">
                        <li>Taming: <%= playerStats[i + 1].taming %></li>
                        <li>Mining: <%= playerStats[i + 1].mining %></li>
                        <li>Woodcutting: <%= playerStats[i + 1].woodcutting %></li>
                        <li>Repair: <%= playerStats[i + 1].repair %></li>
                        <li>Unarmed: <%= playerStats[i + 1].unarmed %></li>
                        <li>Herbalism: <%= playerStats[i + 1].herbalism %></li>
                        <li>Excavation: <%= playerStats[i + 1].excavation %></li>
                        <li>Archery: <%= playerStats[i + 1].archery %></li>
                        <li>Swords: <%= playerStats[i + 1].swords %></li>
                        <li>Axes: <%= playerStats[i + 1].axes %></li>
                        <li>Acrobatics: <%= playerStats[i + 1].acrobatics %></li>
                        <li>Fishing: <%= playerStats[i + 1].fishing %></li>
                        <li>Alchemy: <%= playerStats[i + 1].alchemy %></li>
                    </ul>
                </div>
            </div>
            <% } %>
        </div>

        <script type="text/javascript" src="/assets/main.js"></script>
    </body>
</html>  